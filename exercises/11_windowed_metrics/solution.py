def count_requests_in_window(
    requests: list[tuple[int, str]],
    start: int,
    end: int,
) -> dict[str, int]:
    """Return per-service request counts within an inclusive time window."""
    pass
